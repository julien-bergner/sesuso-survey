<%= link_to "Sesuso", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to t('.logout'), destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to t('.login'), new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to t('.edit_account'), edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to t('.administrate_users'), users_path %>
      </li>
      <li><%= link_to t('.show_all_surveys'), surveys_path %></li>
      <li><%= link_to t('.show_all_survey_locations'), survey_locations_path %></li>
      <li><%= link_to t('.show_all_survey_areas'), survey_areas_path %></li>

<% end %>
  <% else %>
    <li>
    <%= link_to t('.signup'), new_user_registration_path %>
    </li>
  <% end %>
</ul>