<!-- .btn-navbar is used as the toggle for collapsed navbar content -->
<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
</a>

<!-- Be sure to leave the brand out there if you want it shown -->
<div class="brand"><%= t('.project_title') %></div>

<!-- Everything you want hidden at 940px or less, place within here -->
<div class="nav-collapse collapse">

  <ul class="nav">

    <% if user_signed_in? %>
        <li><p class="navbar-text"><%= "#{t('.logged_in_as')}: #{current_user.first_name}" %></p></li>
       <% if current_user.has_role? :admin %>
            <li class="divider-vertical"></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t('.lists') %><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to t('.show_all_surveys'), surveys_path %></li>
                <li><%= link_to t('.show_all_survey_areas'), survey_areas_path %></li>
                <li><%= link_to t('.show_all_survey_locations'), survey_locations_path %></li>
              </ul>
            </li>
            <li class="divider-vertical"></li>
        <% end %>
        <% if current_user.has_role? :agent %>
            <li><%= link_to "Test", :prepare_confirmation_form %></li>
        <% end %>

        <li><%= link_to t('.edit_account'), edit_user_registration_path %></li>
        <% if current_user.has_role? :admin %>
            <li><%= link_to t('.administrate_users'), users_path %></li>
            <li class="divider-vertical"></li>
        <% end %>
            <li><%= link_to t('.logout'), destroy_user_session_path, :method=>'delete' %></li>
    <% else %>
        <li>
          <%= link_to t('.login'), new_user_session_path %>
        </li>
    <% end %>

  </ul>

</div>














